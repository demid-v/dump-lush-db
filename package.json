{
  "name": "dump-lush-db",
  "main": "build/index.js",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch --preserveWatchOutput",
    "lint": "eslint .",
    "start": "npm run build && npm run run --",
    "run": "node ./build/index.js",
    "release": "npm run start:docker && npm run docker:build && docker push demidv/lush-db:latest",
    "start:docker": "npm start -- --split --default-dir",
    "docker:build": "docker build -t demidv/lush-db:latest .",
    "docker:start": "docker start -ai lush-db || npm run docker:run",
    "docker:run": "docker run --name lush-db -p 3307:3307 -e MYSQL_TCP_PORT=3307 -e MYSQL_ROOT_PASSWORD=lush demidv/lush-db:latest"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@typescript-eslint/eslint-plugin": "^5.33.0",
    "@typescript-eslint/parser": "^5.33.0",
    "dotenv": "^16.0.3",
    "eslint": "^8.26.0",
    "typescript": "^4.8.4"
  }
}
